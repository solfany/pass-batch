# application-test.yml 소스코드에 영향은 없으나 jpa로 퀴리를 수행할 때 쿼리를 로깅으로 볼 수 있게 하는 속성들입니다
# sql을 보여주고 sql에 들어가는 파라미터까지 로깅을 해서 보여준다
spring:
jpa:
  show-sql: true
  properties:
    hibernote:
      format_sql: true

logging:
  level:
    org.hibernate.type.descriptor.sql: trace


